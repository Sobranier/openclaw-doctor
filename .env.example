# OpenClaw GA4 Telemetry Secrets
# These values are injected at build time via esbuild `define`.
# The .env file is gitignored — copy this template and fill in your values.
OPENCLAW_GA4_MEASUREMENT_ID=
OPENCLAW_GA4_API_SECRET=
